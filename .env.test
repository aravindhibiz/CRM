# Integration Testing Environment Variables
# 🚨 WARNING: NEVER use production database credentials here!
# 
# For true safety, create a separate Supabase project for testing
# For now, we'll use mocks and read-only operations in production DB

# OPTION 1: Use a separate test database (RECOMMENDED)
# VITE_SUPABASE_TEST_URL=https://your-test-project.supabase.co
# VITE_SUPABASE_TEST_ANON_KEY=your-test-anon-key

# OPTION 2: Use mocked responses (CURRENT - SAFEST)
# Tests will use completely mocked supabase responses
VITE_SUPABASE_TEST_URL=mock://test.supabase.co
VITE_SUPABASE_TEST_ANON_KEY=mock-test-key

# Test Data Prefix (extra safety for any real DB operations)
VITE_TEST_DATA_PREFIX=AUTOMATED_TEST_DO_NOT_DELETE_
